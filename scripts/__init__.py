"""CLI reproducibility scripts for the Geo-BTC paper.

Each script loads ``configs/config.yaml`` by default and exposes a
command-line interface via :mod:`argparse`. Use ``scripts/run_pipeline.py``
to reproduce the entire paper end-to-end.
"""
